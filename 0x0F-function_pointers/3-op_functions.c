#include "3-calc.h"

/**
 * op_add - adding 2 numbers.
 * @a: firt number
 * @b: second number
 * Return: (Result)
*/
int op_add(int a, int b)
{
	return (a + b);
}
/**
 * op_sub - adding 2 numbers.
 * @a: firt number
 * @b: second number
 * Return: (Result)
*/
int op_sub(int a, int b)
{
	return (a - b);

}
/**
 * op_mul - adding 2 numbers.
 * @a: firt number
 * @b: second number
 * Return: (Result)
*/
int op_mul(int a, int b)
{
	return (a * b);

}
/**
 * op_div - adding 2 numbers.
 * @a: firt number
 * @b: second number
 * Return: (Result)
*/
int op_div(int a, int b)
{
	return (a / b);
}
/**
 * op_mod - adding 2 numbers.
 * @a: firt number
 * @b: second number
 * Return: (Result)
*/
int op_mod(int a, int b)
{
	return (a % b);
}
